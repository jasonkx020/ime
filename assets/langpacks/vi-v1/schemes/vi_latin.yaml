# Vietnamese predict — Windows keyboard (special letters + tone bar) + romanized lexicon lookup.
# Composing may contain Unicode Vietnamese letters; engine strips tones for DAT key match.
scheme_id: latin
lang: vi
alphabet: "abcdefghijklmnopqrstuvwxyzăâêôơưđ"
transform:
  type: latin_predict

candidate:
  sources: [lexicon, user, hot]
